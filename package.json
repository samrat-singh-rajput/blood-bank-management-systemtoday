{
  "name": "blood-bank-management-system-workspace",
  "private": true,
  "version": "1.0.0",
  "description": "Workspace root package forwarding scripts to the frontend and backend directories",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run server\"",
    "server": "node backend/server.js",
    "build": "npm run build --prefix frontend",
    "preview": "npm run preview --prefix frontend",
    "install:all": "npm install --prefix frontend && npm install --prefix backend && npm install",
    "typecheck": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
